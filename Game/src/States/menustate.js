// Main menu, this is the first state the player lands on when starting up the game
// Will allow player to choose to play, open settings or quitting
// It will also allow player to select map and playmode

// This gets called from main.js

function MenuState()
{
    this.setup = function()
    {

    }

    this.update = function()
    {
        
    }

    this.setup();
}